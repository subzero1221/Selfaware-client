export enum SettingsField {
  Title = 0,
  Description = 1,
  TimeLimit = 2,
  QuizType = 3,
}

export enum QuizType {
  Knowledge = 0,
  Survey = 1,
}

export enum QuestionType {
  SingleChoice = 0,
  MultipleChoice = 1,
  PsychologicalScale = 2,
}